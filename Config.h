#pragma once

#include <string>

constexpr inline unsigned int WINDOW_WIDTH{1024};  // Example window width
constexpr inline unsigned int WINDOW_HEIGHT{768};  // Example window height
const static inline std::string WINDOW_TITLE{"TactiSim - Mini Tactical Simulator"};

constexpr inline int MAP_TILES_X{32};     // Example map width in tiles
constexpr inline int MAP_TILES_Y{24};     // Example map height in tiles
constexpr inline int TILE_SIZE{32};       // Pixel size of each tile
